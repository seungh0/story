spring:
  data:
    redis:
      port: 6379
      host: localhost
      connect-timeout: 3000
      timeout: 3000

---
spring.config:
  activate:
    on-profile:
      - test

spring:
  data:
    redis:
      host: localhost
      port: 6379
